/**
 * Frontend runtime config.
 * API base is same-origin in production; override for local split setups.
 */
window.BMI_CONFIG = {
  apiBase: '', // e.g. '' or 'http://localhost:3000'
  brand: 'Black Maple Ink',
  siteUrl: 'https://blackmapleink.ca',
  defaultLocale: 'en-CA',
};
